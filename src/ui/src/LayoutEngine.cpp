#include "Drift/UI/LayoutEngine.h"
#include "Drift/UI/UIElement.h"

using namespace Drift::UI;

void LayoutEngine::Layout(UIElement& root)
{
    // Placeholder: chama recalculate layout do root
    // (UIElement já faz cálculo trivial)
    root.Update(0.0f);
} 